{
    "dependencies": {
        "bloatless-react": "^1.2.0",
        "esbuild": "latest",
        "udn-frontend": "^1.0.1"
    },
    "scripts": {
        "build": "esbuild src/index.tsx --bundle --outdir=dist"
    }
}
