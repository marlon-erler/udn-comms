{
  "dependencies": {
    "bloatless-react": "^1.3.1-beta1",
    "esbuild": "latest",
    "udn-frontend": "^1.0.7-beta1"
  },
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --outdir=dist"
  }
}
